import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '跨我身體 OVER MY BODY - Home',
  description: 'Taiwan-based label focusing on avant-garde sounds and experimental compositions.',
};

/**
 * Homepage component - displays the main landing content
 * The main visual background is handled by the layout
 */
export default function HomePage() {
  // Home page content is minimal - the main visual background provides the primary content
  // This preserves the original design where home.html was empty
  return null;
}